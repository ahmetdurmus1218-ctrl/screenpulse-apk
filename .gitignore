*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
# debug.keystore is intentionally committed (see app/build.gradle.kts) so every
# build — local or CI — signs with the same debug key. Without this, home-screen
# widgets break on every reinstall because the signature changes each time.
